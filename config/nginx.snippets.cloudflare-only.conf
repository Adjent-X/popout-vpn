# CF edge filtering is enforced in iptables (INPUT + ipset cloudflare-ipv4)
# so nginx real_ip can restore CF-Connecting-IP without breaking allow/deny.
# Keep mangle/raw 80+443 accepts in sync on the origin host.
allow all;
